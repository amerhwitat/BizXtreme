use std::env;use std::net::{IpAddr,TcpStream};use std::time::Duration;fn main(){let p:u16=env::var("NETWORK_API_PORT").ok().and_then(|x|x.parse().ok()).unwrap_or(8787);println!("{{\"schema\":\"bizxtreme.network.api.v1\",\"implementation\":\"rust\",\"port\":{}}}",p);println!("scope 127.0.0.1 = local/intranet");let _=IpAddr::V4("127.0.0.1".parse().unwrap());println!("local API port reachable = {}",TcpStream::connect_timeout(&format!("127.0.0.1:{}",p).parse().unwrap(),Duration::from_secs(1)).is_ok());}
