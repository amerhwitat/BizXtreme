import 'dart:io';Future<void> main()async{final p=int.tryParse(Platform.environment['NETWORK_API_PORT']??'8787')??8787;print({'schema':'bizxtreme.network.api.v1','implementation':'dart','port':p});print('scope 127.0.0.1 = local/intranet');try{final s=await Socket.connect('127.0.0.1',p,timeout:const Duration(seconds:1));s.destroy();print('local API port reachable');}catch(_){print('local API port not reachable (normal if server not running)');}}
